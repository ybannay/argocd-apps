apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: keycloak
    targetRevision: 9.2.2
    helm:
      parameters:
        - name: auth.adminUser
          value: admin
      values: |
        initContainers:
            - name: theme
              image: youssefbannay1/theme:0.0.1
              imagePullPolicy: Always
              command:
                - sh
              args:
                - -c
                - |
                  echo "Copying My theme..."
                  cp -R /openinnovation-theme/* /theme
              volumeMounts:
                - name: theme
                  mountPath: /theme

        extraVolumes: |
          - name: theme
            mountPath: /theme

        extraVolumeMounts: |
          - name: theme
            mountPath: /opt/bitnami/keycloak/themes                          

  destination:
    server: https://kubernetes.default.svc
    namespace: keycloak
  syncPolicy:
    syncOptions:
    - PruneLast=true
    - CreateNamespace=true 
  revisionHistoryLimit: 1

